export const getTextFromPdf = async (pdfBuffer: Buffer) => {
  // const options = {};
  // const pdfExtract = new PDFExtract();
  // const { text } = await pdfExtract.extractBuffer(pdfBuffer, options);
  // return text;
};
