import { Container } from '@mui/material';
import { RedirectsTable } from './table';

export default function Redirects() {
  return (
    <Container>
      <RedirectsTable />
    </Container>
  );
}
